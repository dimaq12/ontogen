# kotlin-stdlib dialect: prints the genome's fold logic as one Kotlin file,
# compiled by kotlinc (JDK/JBR runtime). The transport (HTTP/queue) is the
# ports layer (D88), orthogonal to the ink; this dialect certifies that the
# LANGUAGE reproduces the canonical fold (fold-parity, the same gate as D48).
