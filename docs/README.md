# Documentation map

- **OPERATOR.md** — run an organism (surfaces, molt, incident runbook).
- **anatomy.html** — the engine anatomy diagram.

## design/ — specification and rationale
- SPEC — architecture; NOT — prohibitions (what we deliberately don't do);
  SCARS — v0 rakes; MODEL — the universal development model; IDEAL — the jump
  spec; STACK — packaging/toolchain; PARADIGM_LIMITS — 11 real-product limits;
  DATAPLANE — the data-plane design (U9); THEORY_BRIDGE — theory↔engine
  verdicts; UNEXPRESSIBLE — expressibility boundaries; TRUST — the execution
  boundary; FREEZE_v1 — the product freeze inventory; SHIP — the packaging plan.

## log/ — running records
- DECISIONS — accepted decisions D1..D87; JOURNAL — dated work log;
  PLAN — phase statuses; PREDICTIONS — falsifiable predictions P1..P16.

## references/ — external sources
- lago-spec.md — the Lago billing reference used as a real-project source.

## spikes/ — early experiments (history)
- expr/ — the Expr-subset experiment behind D23.

The rigorous math corpus lives in `../math/`: Part VII (engine-side
measurements), REVIEW_1 (its adversarial review), Parts I–X + MANIFESTO +
FREEZE_v0.1, and README.md (the map to public sources).
