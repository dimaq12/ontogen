# SHIP: packaging onto for people (wave plan P1-P5)

Goal: in the repo — ONLY working machinery + theory; zero generated junk;
a person from a zero machine gets the tool in 2 honest commands; the harness
hooks up with one (onto init). Every wave — with an exam.

## Decisions on repo composition (inventory done)
STAYS (machinery and theory):
- src/ tests/ tools/ uv.lock pyproject — the engine;
- exams/ — verification machinery, it's PART of the product (37 exams);
- genomes/ + modules/ — the gallery: the language learns by examples;
- cache_skills/ + genomes/.grown/ + genomes/islands/ — CERTIFIED
  artifacts (text, small): they provide fcold/offline determinism —
  this is the "cache is committed" feature, not junk;
- math/ docs/ *.md (SPEC, NOT, SCARS, MODEL, IDEAL, PARADIGM_LIMITS,
  THEORY_BRIDGE, DATAPLANE, FREEZE, DECISIONS, JOURNAL, PREDICTIONS,
  UNEXPRESSIBLE) — theory and protocol;
- life/ — calendar-life template (data already in .gitignore).
DOES NOT LAND (already outside git or add to .gitignore):
- build/ .onto/ __pycache__ — already ignored; ADD: genomes/attest.{json,md}
  (generated by the court), keep .onto/usage_* mentions in the docs.
MOVES: lago/SPEC.md -> docs/references/; spikes/ -> docs/spikes/
  (the D23 decision history — theory, not code).

## P1 — Hygiene and face (a day) — ✅ DONE (LICENSE still pending)
.gitignore top-up; .onto/config.toml.example (no key, with comments
about OpenRouter/ladder); rewrite README for the EXTERNAL reader:
zero machine (uv one-liner -> uv tool install onto), three layers of
dependencies (engine/harness/dialect toolchain — lazy), quickstart
5 commands, link to OPERATOR/FREEZE. LICENSE — the maintainer's decision (a question).
Exam: a lint-guard (git has no pyc/keys/attest artifacts/build).

## P2 — onto init + harness wiring (a day) — ✅ DONE (finit green)
`onto init [--harness claude]`:
- .onto/{cache,ledger,checkpoints}/ + engine.pin + config.toml.example;
- genome/ with a minimal template + flows;
- .mcp.json: registration of onto mcp (Claude Code sees it immediately);
- a CLAUDE.md fragment: "code is not edited — it's proposed into the genome
  (explain -> propose -> court -> materialize)";
- HOOK-GUARD (the killer move): a PreToolUse hook that blocks Edit/Write in
  onto-owned paths (manifest .onto/owned.json, written by materialize) —
  I4 "writes only via propose" as a MECHANICAL barrier of the harness;
- skill onto-workflow (explains the loop to the harness).
The ownership boundary is CLEAN: onto owns genome/ + .onto/ + the materialized
output; the rest of the repo is human. Exam finit: a tmp project ->
init -> the hook blocks a direct edit of an owned file, propose passes,
court green.

## P3 — uv package — ✅ DONE (fship 10/10)
`uv build` + install into a clean tool-env; FOUND GAP: onto new/init
take templates from repo paths (v1root) — will break on the installed package;
templates -> package data (src/onto/templates_gallery/). Check
python-requires, entry point outside the repo. Exam: install the wheel into a clean
env -> onto version/init/court/serve on a template WITHOUT the repo and WITHOUT network.

## P4 — Delivery exam fship (half a day, in CI) — ✅ DONE (fship in CI)
End-to-end: a fresh tmp "machine" -> local wheel -> init -> court ->
serve -> judge -> attest; the repo lint-guard; the pin protocol
(engine.pin mismatch -> warning).

## P5 — Publication (the maintainer's decisions + half a day) — PENDING
Package name (PyPI) and public repo; license; extraction of v1 ->
a separate public repo (NOT §33: the engine separate from the working cauldron;
dnaContract remains the lab). PyPI publication or git install
(uv tool install git+...) at first. We do NOT make a bootstrap script:
honest 2 steps + pin (PARADIGM_LIMITS §11 — we don't drag curl|sh into the trust base).

## Open questions (don't block P1-P4)
1) License? 2) Package/repo name (onto is almost certainly taken on PyPI —
   candidates?) 3) PyPI right away or git install at the start?
